 <h5>{{otra}}</h5>
 <ul class="list-group">
  {{#each comentarios}}
    <li class="list-group-item">{{comentario}} ------------- {{puntaje}}
      <button type="button" type="submit" class="btn btn-default" data-id="{{id}}"  id="dddd" onclick="borraComentario({{id}})">Eliminar</button>
</li>
  {{/each}}
</ul>
